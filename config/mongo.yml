defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: fact_cave_development

test:
  <<: *defaults
  database: fact_cave_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: fact_cave
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
