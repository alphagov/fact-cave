<div class="page-header">
  <h1>Editing <%= @fact.name %></h1>
</div>

<%= form_for @fact, :url => admin_fact_path(@fact), :method => 'put',
                    :html => { :class => 'form-horizontal' } do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end %>
